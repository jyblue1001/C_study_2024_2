    // printf("1st, size of array_A is %d, size of array_B is %d\n", sizeof(array_A), sizeof(array_B));

    // min_size = (size_A > size_B) ? size_B : size_A;

    // if (size_A > size_B){
    //     *array_B = (int *)realloc(*array_B, sizeof(int)*size_A);
    // }
    // else if (size_B > size_A){
    //     *array_A = (int *)realloc(*array_A, sizeof(int)*size_B);
    // }

    // temp_array_address = *array_A;
    // *array_A = *array_B;
    // *array_B = temp_array_address;    